export interface PortfolioData {
  id: number;
  title: string;
  img: string;
}

export interface UserData {
  id: number;
  name: string;
  email: string;
  password: string;
  role: string;
  gender: string;
  confirm: string;
}
